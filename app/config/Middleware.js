import React, { Component } from 'react';
import Data from './Data';

const data = new Data();
export default class Middleware extends Middleware {
    constructor(props) {
        super(props);
    }

    role() {

    }
}
